services:
  - type: web
    name: trip-scraper-backend
    env: node
    branch: main
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: NODE_VERSION
        value: 18
